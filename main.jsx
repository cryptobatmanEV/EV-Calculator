import React from "react";
import ReactDOM from "react-dom/client";
import EVCalculator from "./ev-calculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EVCalculator />
  </React.StrictMode>
);
